#pragma once

#include <Common.h>

static const int SCREEN_WIDTH = 640; //static variables for the window size
static const int SCREEN_HEIGHT = 575;

void InitializeUI();
void HandleUIEvents();
void UpdateUI();
