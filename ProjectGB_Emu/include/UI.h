#pragma once

#include <Common.h>

static const int SCREEN_WIDTH = 1024; //static variables for the window size
static const int SCREEN_HEIGHT = 768;

void InitializeUI();
void HandleUIEvents();
void UpdateUI();
